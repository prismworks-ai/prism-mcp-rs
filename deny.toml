# Configuration for cargo-deny
# More info: https://github.com/EmbarkStudios/cargo-deny

[graph]
targets = [
    { triple = "x86_64-unknown-linux-gnu" },
    { triple = "x86_64-apple-darwin" },
    { triple = "x86_64-pc-windows-msvc" },
]

[licenses]
# List of allowed licenses
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-DFS-2016",
    "CC0-1.0",
    "MPL-2.0",
    "Unlicense",
]

# Modern license configuration
# Removed deprecated keys: copyleft, allow-osi-fsf-free, default
confidence-threshold = 0.8
private = { ignore = true }

[bans]
# Deny multiple versions of the same crate
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"

# List of explicitly allowed duplicate crates
allow = [
    # Common duplicates that are hard to avoid
    { name = "bitflags" },
    { name = "syn" },
]

# Skip certain crates entirely
skip = [
    # Dev dependencies that we don't care about
    { name = "criterion" },
]

# Similarly named crates that are allowed
skip-tree = [
    # Skip dev-dependencies
    { name = "criterion" },
]

# Explicitly deny problematic licenses
deny = [
    # Deny GPL and copyleft licenses
    { name = "*", license = "GPL-2.0" },
    { name = "*", license = "GPL-3.0" },
    { name = "*", license = "AGPL-3.0" },
    { name = "*", license = "LGPL-2.0" },
    { name = "*", license = "LGPL-3.0" },
]

[advisories]
# The path where the advisory database is cloned/fetched into
db-path = "~/.cargo/advisory-db"

# The url(s) of the advisory databases to use
db-urls = ["https://github.com/rustsec/advisory-db"]

# Modern advisory configuration
# Removed deprecated keys: vulnerability, notice
# These are now handled by the default severity levels
unmaintained = "warn"

# A list of advisory IDs to ignore
ignore = [
    # Example: "RUSTSEC-2019-0001",
]

[sources]
# Lint level for what to happen when a crate from a crate registry that is not in the allow list is encountered
unknown-registry = "warn"

# Lint level for what to happen when a crate from a git repository that is not in the allow list is encountered
unknown-git = "warn"

# List of allowed crate registries
allow-registry = ["https://github.com/rust-lang/crates.io-index"]

# List of allowed Git repositories
allow-git = []